import 'package:flutter/material.dart';

class IconTextItem extends StatelessWidget {
  final String text;
  final String iconRes;

  IconTextItem(this.iconRes, this.text);

  @override
  Widget build(BuildContext context) {
    return new Row(

     
    );
  }
}
